version: '3'

silent: true

tasks:

  build:
    desc: Builds the book
    cmds:
      - cmd: mdbook build

  clean:
    desc: Deletes a built book
    cmds:
      - cmd: mdbook clean

  serve:
    desc: Serves the book locally
    cmds:
      - cmd: mdbook serve
